function isEven(value) {
    if (value % 2 === 0) {
        return true;
    }
    else
        return false;
    }


//what is the big O of the algorith? Explain your answer.

//Answer: O(1) - constant, because it has the same amount of operations regardless of input value.